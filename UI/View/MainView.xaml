<Window x:Class="UI.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.View"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        WindowStyle="None" 
        BorderBrush="Black"
        BorderThickness="5"
        AllowsTransparency="True"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Column="0"  Content="Print" HorizontalAlignment="Center" VerticalAlignment="Center" Height="434" Width="400" Click="Print_Click"  />
        <Button Grid.Column="1"  Content="Send" HorizontalAlignment="Center" VerticalAlignment="Center" Height="434" Width="400"   Click="Send_Click" />

    </Grid>
</Window>
